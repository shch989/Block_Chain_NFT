// SPDX-License-Identifier: MIT
pragma solidity >= 0.7.0 < 0.9.0;

contract learnVariables {
    // 정수 변수 chocolateBar를 10으로 초기화합니다.
    uint chocolateBar = 10;

    // 정수 변수 storeOwner를 300으로 초기화합니다.
    uint storeOwner = 300;

    // 불린 변수 lieDetector를 true로 초기화합니다.
    bool lieDetector = true;

    // 문자열 변수 errorMessageText를 "Error! There has been a mistake!"로 초기화합니다.
    string errorMessageText = "Error! There has been a mistake!";

    // 정수 변수 wallet을 500으로 초기화합니다.
    uint wallet = 500;

    // 불린 변수 spend를 false로 초기화합니다.
    bool spend = false;

    // 문자열 변수 motifySpend를 "you have spent money"로 초기화합니다.
    string motifySpend = "you have spent money";
}